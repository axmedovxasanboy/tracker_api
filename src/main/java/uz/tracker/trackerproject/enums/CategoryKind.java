package uz.tracker.trackerproject.enums;

/**
 * Drives kind-specific UI extras on the transaction form. New kinds get a stable
 * identifier here and matching conditional rendering on the client.
 */
public enum CategoryKind {
    GENERIC,
    FOOD,
    TRANSPORT
}
